// 두 값을 합산하는 함수
int Sum(int a, int b) {
    return a + b;
}

// 두 절대값을 합산하는 함수
int SumABC(int a, int b) {
    // 음수이면 -1을 곱해서 양수로 만듬
    if (a < 0) a = a * (-1);
    if (b < 0) b = b * (-1);

    return a + b;
}
