#include "sum.h"
#pragma comment(lib, "sum.lib")

int main() {

    int result1, resutl2;

    // result1에는 -1이 저장됨
    result1 = Sum(2, -3);

    // result2에는 5가 저장됨
    result2 = SumABC(2, -3);

    return 0;
}
