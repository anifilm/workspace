// 변수 알아보기 2
#include <stdio.h>

int main() {

    int a;
    a = 127;

    printf("a의 값은 %d진수로: %o\n", 8, a);
    printf("a의 값은 %d진수로: %d\n", 10, a);
    printf("a의 값은 %d진수로: %x\n", 16, a);

    return 0;
}
