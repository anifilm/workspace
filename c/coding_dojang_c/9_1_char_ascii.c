#include <stdio.h>

int main() {

    char c1 = 97;   // a의 ASCII 10진수 코드값 97
    char c2 = 98;   // b의 ASCII 10진수 코드값 98

    printf("char c1: %c, %d\n", c1, c1);     // %c 문자열, %d 정수값 출력
    printf("char c2: %c, %d\n", c2, c2);     // %c 문자열, %d 정수값 출력

    return 0;
}
