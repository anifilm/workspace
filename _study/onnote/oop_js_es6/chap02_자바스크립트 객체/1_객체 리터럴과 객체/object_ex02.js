// 1.2.1 객체 생성시 정의

let coffee = {
  name: 'Americano',
  price: 3000,
};

console.log(coffee.name);  // Americano
console.log(coffee.price); // 3000

console.log(coffee['name']);  // Americano
console.log(coffee['price']); // 3000
